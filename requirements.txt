aiogram==3.15.0
aiosqlite==0.20.0
cryptography==43.0.3
python-dotenv==1.0.1

# Неофициальный API Playerok. В PyPI его нет, ставится из GitHub —
# значит, в окружении сборки обязан быть git.
# Если сборка падает с "git: command not found" — библиотеку придётся
# положить в репозиторий папкой playerokapi/ и убрать эту строку.
playerokapi @ git+https://github.com/alleexxeeyy/PlayerokAPI.git
